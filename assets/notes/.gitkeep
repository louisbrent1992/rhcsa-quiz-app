Study notes generated by tool/extract_notes.py land here as chNN.json.

They are the textbook's own prose, so they are gitignored and must not be
committed: this repository is public. Regenerate them locally with

    python3 tool/extract_notes.py

The app works without them - Learn mode falls back to the command reference
and flashcards built from assets/questions/.
